# Netlify redirects. Copied into dist/ on build.
# Serve pages at clean paths (200 = rewrite, URL stays clean).
/admin       /admin.html       200
/products    /products.html    200
/gallery     /gallery.html     200
